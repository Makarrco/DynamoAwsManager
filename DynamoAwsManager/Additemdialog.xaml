<Window x:Class="DynamoAwsManager.AddItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add / Edit Item"
        Height="500" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#0F172A">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="13"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background"      Value="#1E293B"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderBrush"     Value="#475569"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding"         Value="8,6"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Margin"          Value="0,0,0,8"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="14,8"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Fill in the attribute values. Add extra attributes below."
                   Foreground="#94A3B8" Margin="0,0,0,14" TextWrapping="Wrap"/>

        <!-- Fixed key fields injected from code -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="FieldsPanel"/>
        </ScrollViewer>

        <!-- Add extra attribute row -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="ExtraKeyBox"   Grid.Column="0" Margin="0,0,6,0" Height="34"/>
            <TextBox x:Name="ExtraValueBox" Grid.Column="1" Margin="0,0,6,0" Height="34"/>
            <Button  Content="+ Add"        Grid.Column="2" Background="#334155"
                     Height="34" Width="70" Click="AddExtraAttr_Click"/>
        </Grid>
        <TextBlock Grid.Row="2" Text="  Attr name             Value"
                   Foreground="#475569" FontSize="11" Margin="0,-18,0,0"
                   IsHitTestVisible="False"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="90" Background="#475569" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button Content="Save"   Width="90" Background="#10B981" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
